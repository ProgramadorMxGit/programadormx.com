# Publica programadormx.com en GitHub Pages en cada commit a main.
# Corre las pruebas, construye dist/ con tools/build.py y la despliega.
# Si una prueba o el build fallan, no se publica nada y la web sigue como estaba.
name: Publicar web

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  construir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Pruebas
        run: python -m unittest discover -s tools/tests -t .

      - name: Construir dist/
        run: python tools/build.py

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  publicar:
    needs: construir
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.despliegue.outputs.page_url }}
    steps:
      - id: despliegue
        uses: actions/deploy-pages@v4
